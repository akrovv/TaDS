#ifndef __SORTS_H__
#define __SORTS_H__

void selection_sort_by_fields(travel_t countries[], size_t len);
void selection_sort_by_keys(key_travel_t keys[], size_t len);
void merge_sort_by_fields(travel_t countries[], size_t len);

#endif
